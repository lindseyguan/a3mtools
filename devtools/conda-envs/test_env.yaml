name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python>=3.10
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy
  - pandas
  - biopython>=1.8
  - importlib-resources

    # Pip-only installs
  - pip:
    - -e ../../

